---

name: meihua  
description: 中文PPT美化与图片版课件生成工作流。Use when the user provides a paginated PPT/PPTX, slide text, page screenshots, or current slide images and asks Codex to beautify, redesign, polish, visualize, or remake every slide while preserving original copy and page-specific images, then optionally merge the generated page images into a PPTX. Especially useful for Chinese government/business/courseware/report decks where the user wants two style directions first, then page-by-page image generation, review, revision, and final PPT delivery.

---

# Meihua

## 核心目标

把用户提供的“已分页但不够美观”的 PPT，转化为一套视觉统一、逻辑结构清晰、充分尊重原文案和原页图片素材的图片版幻灯片；完成全部页面后，再按顺序合并为一个 PPTX。

优先做“设计师式重绘”，不是简单套模板：保留每页原有文案、数据、图表、照片、页面逻辑，只优化视觉层级、信息分组、排版、色彩、装饰与阅读路径。

## 工作原则

- 先理解内容场景，再设计风格。不要一上来逐页生成。
- 一开始必须给用户两套视觉方案，并推荐更适合的一套；等用户选择或认可后再批量制作。
- 尊重原文案。除非用户明确要求精简，否则不要主动删除、改写、概括关键句、数据、标题、表格字段。
- 尊重原页图片。每页已有照片、地图、表格截图、规划图、视频截图等素材，必须引用到生成页面中。
- 当两页文案相同或同级但图片不同，必须保持版式、字号、间距、标题位置、装饰线、图文比例一致，只替换对应图片和必要文字。
- 章节页同级统一：标题字号、标题位置、副标题字号、深色底板/刷痕、装饰线、背景图处理必须一致。
- 用户反馈优先于原生成结果。若用户指出某段原文已删除、某处重复、某页不统一，应按最新 PPT 或用户标注重新修订并覆盖对应页。
- 输出以图片页为母版。最终 PPT 每页放一张全屏图片，避免字体缺失、版式漂移、元素重排。
- 禁止使用python之类的代码来生成页面，必须使用gpt-image-2模型来生成
- 严禁用代码、程序化图形、PPT形状、HTML/CSS/Canvas、截图模板等方式生成“图片版PPT页面”。这些方式只能用于读取、检查、保存、合并已经完成的图片页，不能替代逐页图片生成与设计师式重绘。
- 严禁先生成无文字背景图或无文字图片页，再用后期叠字方式伪装成“带文字图片页”。每一页成品图必须在生成阶段直接包含该页最终可见文字、标题、卡片、流程、图标和视觉背景；除非用户明确要求制作无文字背景素材。

## 标准流程

### 1. 读取素材

定位用户提供的 PPT/PPTX、文案、截图、图片文件夹或参考风格图。若用户只说“这个文件夹”，先查看目录，找到最新版本文件。

需要提取：

- 每页标题、正文、数据、表格、备注和页面图片。
- 每页原始截图或渲染预览，作为“当前页文案和图片素材”的权威参考。
- 页面之间的关系：封面、目录、章节页、内容页、案例页、数据页、结尾页，以及同文不同图的成组页面。

如需处理 PPT/PPTX，结合 `presentations` skill 读取、渲染和检查。若任务是中文课程化/图片化页面，结合 `generate-courseware` skill 做视觉转化。

### 2. 先给两套风格方案

基于内容场景提出两套视觉方向，每套包含：

- 风格名。
- 适用理由。
- 色彩、字体、版式、图像处理、装饰语言。
- 适合哪些页面类型。
- 潜在风险。

必须给出推荐方案，说明为什么更适合当前内容与汇报对象。

可以先生成 3 张概念图或封面样张供用户选择。概念图应使用真实内容方向，不要做空泛海报。

### 3. 建立设计系统

在用户确认风格后，固定以下规则：

- 画布：16:9，优先 1920x1080 PNG。
- 标题层级：封面、章节页、一级标题、卡片标题、正文、数据数字分别设定一致大小与颜色。
- 页面类型模板：封面、目录、章节页、双卡对比页、证据图片页、案例页、数据表页、路线图页、结尾页。
- 色彩与装饰：主色、强调色、背景纹理、分割线、图标、底部景观或品牌元素。
- 图片策略：原图优先；必要时只做裁切、统一色调、加遮罩或边框，不替换事实性图片。

### 4. 逐页生成图片版页面

每页生成时都要：

- 使用该页原始截图或提取图片作为参考图。
- 在提示词中写明“必须保留当前页图片素材”和“不要新增无关文字”。
- 尽可能完整保留原文案，尤其是数字、地名、政策表述、案例名、表格标题。
- 做视觉和逻辑结构化：分栏、编号、卡片、时间线、账本、对比、流程、地图标注、图文证据区等。
- 将生成图保存为连续命名：`slide-01.png`、`slide-02.png` ...

对于同组页面：

- 先生成模板页。
- 后续页引用模板页作为风格参考。
- 明确提示“只替换照片/地图/案例图，其他排版尽量完全一致”。

### 5. 中途检查与修订

每生成若干页，向用户简短说明进度。用户反馈时：

- 明确是哪一页。
- 按用户最新要求修订对应图片。
- 覆盖保存到同一个 `slide-XX.png`，不要产生混乱版本。
- 若反馈影响同类页面，例如章节页字号、成组页版式，要同步更新后续规则，必要时回改已完成页。

常见修订包括：

- 删除原文已删的残留段落或数字。
- 去除重复表达。
- 统一同级字号和排版。
- 保持两页只换图不换布局。
- 调整底部空白、视觉重心、图片裁切、标题可读性。

### 6. 合并为 PPTX

完成全部图片页后，按顺序合成 PPT：

- 每页一张已经包含最终文字、图片、背景与全部视觉元素的全屏 PNG。
- 不额外叠加可编辑文字层，除非用户明确要求。
- 保持 16:9 宽屏比例。
- 合并后检查页数、顺序、是否黑页、是否漏页。

禁止默认使用脚本或程序化生成方式自动合成 PPTX；只有在用户明确授权“可以用脚本/程序合成”时才允许使用。若用户要求“直接生成图片带文字的 PPT”，必须先完成每页带文字的成品图片，再把图片逐页放入 PPT。

## 推荐输出结构

在项目输出目录中使用：

```text
courseware-output/
  项目名/
    images-generated/
      slide-01.png
      slide-02.png
      ...
    work/
      slide-text-summary.json
      rendered-previews/
      ppt-build/
    项目名-图片版.pptx
```

## 质量检查清单

交付前检查：

- 图片页数量是否等于原 PPT 页数。
- 文件名是否连续。
- 每页是否保留该页关键文案、数字和图片。
- 同文不同图页面是否布局一致。
- 章节页是否同级统一。
- 用户指出的删除/修改点是否已覆盖。
- 最终 PPT 页数是否正确。
- 从 PPT 反向渲染或抽查预览是否无黑页、无漏页、无明显裁切错误。

## 回复方式

工作过程中用简短中文进度说明，不要把技术细节堆给用户。最终回复只说明：

- 已完成什么。
- 成品图片目录。
- 成品 PPT 路径。
- 已做哪些关键检查。

如果存在无法验证的环节，要明确说明。
